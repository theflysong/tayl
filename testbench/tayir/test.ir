import std

/*

int main(int argc, const char **argv) {
    int a, b;
    scanf("%d%d", &a, &b);
    c = a + b * 2;
    printf("%d\n", c);
    return 0;
}

*/

const_string @cs$annoymous$0 = "%d%d";
const_string @cs$annoymous$1 = "%d\n";

def @main(i32 %argc, p64 %argv) -> i32 {
start:
    // because we use scanf, so we use instruction `alloc` to alloc space from stack
    %a$ptr = alloc #i32
    %b$ptr = alloc #i32
    call @scanf, [@cs$annoymous$0, %a$ptr, %b$ptr]
    %a$val = load %a$ptr
    %b$val = load %b$ptr
    %temp$0 = mul %b$val, 2
    %c = add %a$val, %temp$0
    call @printf, [@cs$annoymous$1, %c]
    ret 0
}